use Mix.Config

if Mix.env == :test do
  args = %{
    adapter: Ecto.Adapters.Postgres,
    username: "postgres",
    password: "postgres",
    database: "fire_sale_test",
    pool: Ecto.Adapters.SQL.Sandbox
  }
else
  args = %{
    adapter: Ecto.Adapters.Postgres,
    username: "postgres",
    password: "postgres",
    database: "fire_sale_#{Mix.env}",
    pool_size: 10
  }
end

config :fire_sale, FireSale.Repo, args
